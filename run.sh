# create env variables
# export APP_DIR=`pwd`
# launch supervisor
# supervisord -n -c $APP_DIR/tracker/config/supervisord.conf
# launch supervisor monitor
# supervisorctl -c $APP_DIR/tracker/config/supervisord.conf

# full command to run with ENV variable
# python setup.py install; python setup.py build; supervisord -n -c $APP_DIR/tracker/config/supervisord.conf